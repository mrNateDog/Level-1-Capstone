function jumpA(comps) {
  var top = document.getElementById(comps).offsetTop;
  window.scrollTo(0, top);
}
function jumpB(exp) {
  var top = document.getElementById(exp).offsetTop;
  window.scrollTo(0, top);
}
function jumpC(edu) {
  var top = document.getElementById(edu).offsetTop;
  window.scrollTo(0, top);
}
